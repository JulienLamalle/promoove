<section class="py-10 pt-md-12 pb-12 pb-md-15 overflow-hidden ">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-md-10 col-lg-8 text-center">

        <!-- Headin -->
        <h1 class="display-1 font-weight-bold animated fadeInUp slow">
          Les langages.
        </h1>

        <!-- Text -->
        <p class="mb-4 animated fadeInUp delay-1s slow">
          Trouvez aisément les projets qui vous tiennent à coeur et soutenez-les.
        </p>

        <nav class="nav justify-content-center animated fadeInUp delay-1-5s slow">
          <% @languages.each do |language| %>
            <%=link_to language_path(language), class: "badge badge-em badge-pill mr-2 mb-3 bg-gradient-info" do%>
              <span class="h6 text-uppercase text-white"><%= language.name %></span>
            <% end %>
          <% end %>
        </nav>
      </div>
    </div> <!-- / .row -->
  </div> <!-- / .container -->
</section>
